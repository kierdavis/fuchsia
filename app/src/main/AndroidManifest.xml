<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kierdavis.fuchsia">

    <!-- TODO allow usage on devices without a camera -->
    <uses-feature android:name="android.hardware.camera" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <!-- TODO auto thumbnails for collections -->
    <!-- TODO support adding items to collections from the item editor page -->
    <!-- TODO allow pictures to be cropped/edited before importing -->
    <!-- TODO implement intent magic to allow you to share a photo to Fuchsia from another app and seed creation of a new item -->
    <!-- TODO allow items to be removed from collections -->
</manifest>